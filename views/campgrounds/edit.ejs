<% layout("layouts/boilerplate") %>
    <div class="row">
        <h1 class="text-center">Edit Campground</h1>
        <div class="col-6 offset-3">
            <form action="/campgrounds/<%= campground._id %>?_method=PUT" method="post" novalidate
                class="validated-form">
                <div class="mb-3">
                    <label for="title" class="form-label">Title</label>
                    <input type="text" name="campground[title]" required id="title" class="form-control"
                        value="<%= campground.title %>">
                    <div class="valid-feedback">
                        Good to go!!
                    </div>
                </div>
                <div class=" mb-3">
                    <label for="location" class="form-label">Location</label>
                    <input type="text" name="campground[location]" required id="location" class="form-control"
                        value="<%= campground.location %>">
                    <div class="valid-feedback">
                        Good to go!!
                    </div>
                </div>
                <div class="mb-3">
                    <label for="image" class="form-label">Image url</label>
                    <input type="text" name="campground[image]" required id="image" class="form-control"
                        value="<%= campground.image %>">
                    <div class="valid-feedback">
                        Good to go!!
                    </div>
                </div>

                <div class="mb-3">
                    <label for="price" class="form-label">Campground Price</label>
                    <div class="input-group">
                        <span class="input-group-text" id="basic-addon1">$</span>
                        <input type="text" class="form-control" name="campground[price]" id="price" placeholder="0.00"
                            aria-label="text" aria-describedby="price-label" required value="<%= campground.price %>">
                        <div class="valid-feedback">
                            Good to go!!
                        </div>
                    </div>
                </div>

                <div class="mb-3">
                    <label for="description" class="form-label">Description</label>
                    <textarea type="text" name="campground[description]" required id="description"
                        class="form-control"><%= campground.description %></textarea>
                    <div class="valid-feedback">
                        Good to go!!
                    </div>
                </div>
                <div class="mb-3">
                    <button class="btn btn-info">Update Campground</button>
                </div>

            </form>

            <a href="/campgrounds">All Campgrounds</a>
            <a href="/campgrounds/<%= campground._id %> ">Back to Campground</a>
        </div>
    </div>